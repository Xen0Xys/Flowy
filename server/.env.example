DATABASE_URL="postgres://postgres:postgres@localhost:5432/postgres"
APP_NAME=Flowy
APP_SECRET=supersecret
CORS_ORIGINS=
# Optional: WebAuthn Relying Party ID (domain only, no scheme/port).
# When unset, derived from the first CORS_ORIGINS entry hostname.
WEBAUTHN_RP_ID=
